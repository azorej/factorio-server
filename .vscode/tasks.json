{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Update Dagger Python venv",
            "type": "shell",
            "command": [
                "${workspaceFolder}/scripts/update_dagger_venv.sh"
            ],
        },
        {
            "label": "Login to Dagger Cloud",
            "type": "shell",
            "command": [
                "dagger login"
            ],
        }
    ]
}
